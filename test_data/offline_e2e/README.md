# Offline E2E fixture

Small anonymized FA(3) fixture set for offline end-to-end tests.

It covers:

- regular sales invoice,
- purchase invoice,
- correction to zero with `StanPrzed`.

The data is synthetic and must not contain real KSeF runtime exports or secrets.
