# PROJECT SNAPSHOT FOR AI

Generated: 2026-04-29T14:18:47
Root: ksef-send
Files included: 29
Directories skipped: 6
Files skipped: 8
Total skipped paths: 14
Total lines: 2679

# INSTRUKCJE DLA AI

To jest snapshot projektu Python do wysyłania faktur sprzedaży do KSeF.

Przeanalizuj projekt jako całość, nie jako pojedyncze niezależne pliki.

Ważne:
- Najpierw zrozum strukturę projektu i przepływ danych.
- Respektuj znaczniki FILE START / FILE END.
- Przy sugestiach podawaj dokładne ścieżki plików.
- Szczególnie sprawdzaj bezpieczeństwo wysyłki produkcyjnej.
- Szczególnie sprawdzaj ryzyko podwójnej wysyłki faktury.
- Szczególnie sprawdzaj manifest, ledger, statusy i obsługę błędów.
- Nie proponuj realnej wysyłki do KSeF bez jasnych zabezpieczeń.
- Nie zakładaj istnienia plików, których nie ma na liście.
- Nie przepisuj całego projektu, jeśli nie zostaniesz o to poproszony.
- Jeśli zauważysz możliwe sekrety, tokeny, NIP-y, klucze lub dane prywatne, ostrzeż mnie.


# PROJECT CONTEXT

Project name:
ksef-send

Purpose:
Local Python CLI tool for validating, preparing, sending and archiving sales invoices through KSeF.

Business purpose:
Sending Polish sales invoices to KSeF with safety checks, audit trail, PDF preview, final PDF generation, QR code and email package preparation.

Inputs:
- sales invoice XML files in outbound/pending
- KSeF token from .env
- KSeF public key
- local configuration from .env
- optional PDF preview files

Outputs:
- validated XML invoices
- send manifest
- session ledger
- dry-run logs
- KSeF response logs
- archived invoice packages
- final PDF with KSeF number
- QR code
- email package for customer

Type:
CLI / local automation tool.

Main entrypoint:
main.py

Status:
Personal/local business tool in active development.

Important safety assumption:
Real production sending must be blocked unless explicitly enabled by ALLOW_PROD_SEND=true and operator confirmation.


# PROJECT ARCHITECTURE

Entry point:
- main.py

Main domains:
- configuration and environment validation
- XML validation
- business rules validation
- invoice approval pipeline
- KSeF authentication
- dry-run sending
- real KSeF sending
- KSeF status polling
- manifest management
- session ledger / audit trail
- PDF preview and final PDF generation
- QR code generation
- email package preparation
- archiving

Core files:
- main.py
- config.py
- auth.py
- http_client.py
- utils.py

Validation:
- validator/validate_fa.py
- validator/business_rules.py
- validator/xsd/*

Sending:
- sender/ksef_sender.py
- sender/ksef_real_send.py
- sender/ksef_online_session.py
- sender/ksef_status_poll.py
- sender/send_invoice.py
- sender/send_manifest.py
- sender/session_ledger.py
- sender/session_lock.py

PDF / QR / email:
- sender/pdf_preview.py
- sender/final_pdf.py
- sender/qr_code.py
- sender/email_package.py
- sender/invoice_summary.py

Archive:
- sender/archive.py

Tools:
- tools/extract_token_cert.py
- tools/inspect_keys.py

Important runtime directories:
- outbound/pending
- outbound/approved
- outbound/sent
- outbound/rejected
- outbound/logs
- outbound/auth
- outbound/archive
- outbound/preview
- outbound/final_pdf
- outbound/email

Sensitive files:
- .env
- keys/*
- outbound/auth/*
- tokens
- KSeF credentials


# DATA FLOW

Sales invoice XML
    ↓
outbound/pending
    ↓
validator/validate_fa.py
    ↓
validator/business_rules.py
    ↓
sender/pdf_preview.py
    ↓
outbound/approved
    ↓
sender/send_manifest.py
    ↓
Dry run OR real KSeF send

Dry run:
outbound/approved
    ↓
sender/ksef_sender.py
    ↓
outbound/sent
    ↓
outbound/logs

Real send:
outbound/approved
    ↓
sender/ksef_real_send.py
    ↓
auth.py / KSeFAuthClient
    ↓
http_client.py
    ↓
KSeF API
    ↓
session reference + invoice reference
    ↓
status check / polling
    ↓
KSeF reference number
    ↓
outbound/sent
    ↓
sender/send_manifest.py
    ↓
sender/final_pdf.py
    ↓
sender/qr_code.py
    ↓
sender/email_package.py
    ↓
sender/archive.py

Audit:
KSeF events
    ↓
sender/session_ledger.py
    ↓
outbound/session_ledger.json

Safety:
real send
    ↓
ALLOW_PROD_SEND
    ↓
operator confirmation
    ↓
PDF confirmation
    ↓
duplicate check by filename and SHA256
    ↓
session lock


# MODULE DEPENDENCIES

auth.py
  └── config.py
  └── http_client.py
  └── utils.py

main.py
  └── auth.py
  └── config.py
  └── http_client.py
  └── sender/archive.py
  └── sender/ksef_online_session.py
  └── sender/ksef_real_send.py
  └── sender/ksef_sender.py
  └── sender/ksef_status_poll.py
  └── sender/pdf_preview.py
  └── sender/send_invoice.py
  └── sender/send_manifest.py
  └── validator/business_rules.py
  └── validator/validate_fa.py

send-ksef.py
  └── config.py
  └── main.py
  └── sender/ksef_sender.py

sender/archive.py
  └── config.py

sender/email_package.py
  └── config.py
  └── sender/invoice_summary.py
  └── sender/qr_code.py

sender/final_pdf.py
  └── config.py
  └── sender/qr_code.py

sender/ksef_online_session.py
  └── auth.py
  └── config.py
  └── http_client.py
  └── sender/archive.py
  └── sender/ksef_sender.py
  └── sender/send_invoice.py
  └── sender/send_manifest.py

sender/ksef_real_send.py
  └── auth.py
  └── config.py
  └── http_client.py
  └── sender/ksef_sender.py
  └── sender/send_invoice.py
  └── sender/send_manifest.py
  └── sender/session_ledger.py

sender/ksef_sender.py
  └── config.py
  └── sender/send_invoice.py
  └── sender/send_manifest.py
  └── utils.py

sender/ksef_status_poll.py
  └── auth.py
  └── config.py
  └── http_client.py
  └── sender/send_manifest.py
  └── sender/session_ledger.py

sender/pdf_preview.py
  └── config.py

sender/qr_code.py
  └── config.py

sender/send_manifest.py
  └── config.py
  └── sender/email_package.py
  └── sender/final_pdf.py

sender/session_ledger.py
  └── config.py

validator/business_rules.py
  └── config.py


# PROJECT TREE

.
├── .env.example
├── .gitignore
├── auth.py
├── config.py
├── http_client.py
├── ISSUE1.md
├── main.py
├── README.md
├── requirements.txt
├── send-ksef.py
├── sender/
│   ├── archive.py
│   ├── email_package.py
│   ├── final_pdf.py
│   ├── invoice_summary.py
│   ├── ksef_online_session.py
│   ├── ksef_real_send.py
│   ├── ksef_sender.py
│   ├── ksef_status_poll.py
│   ├── load_invoice.py
│   ├── pdf_preview.py
│   ├── qr_code.py
│   ├── send_invoice.py
│   ├── send_manifest.py
│   ├── session_ledger.py
├── tools/
│   ├── extract_token_cert.py
│   ├── inspect_keys.py
├── utils.py
├── validator/
│   ├── business_rules.py
│   ├── validate_fa.py

# INCLUDED FILES

- .env.example
- .gitignore
- auth.py
- config.py
- http_client.py
- ISSUE1.md
- main.py
- README.md
- requirements.txt
- send-ksef.py
- sender/archive.py
- sender/email_package.py
- sender/final_pdf.py
- sender/invoice_summary.py
- sender/ksef_online_session.py
- sender/ksef_real_send.py
- sender/ksef_sender.py
- sender/ksef_status_poll.py
- sender/load_invoice.py
- sender/pdf_preview.py
- sender/qr_code.py
- sender/send_invoice.py
- sender/send_manifest.py
- sender/session_ledger.py
- tools/extract_token_cert.py
- tools/inspect_keys.py
- utils.py
- validator/business_rules.py
- validator/validate_fa.py

# SKIPPED PATHS

- .git/ — ignored directory
- __pycache__/ — ignored directory
- outbound/ — ignored directory
- sender/__pycache__/ — ignored directory
- validator/__pycache__/ — ignored directory
- validator/xsd/ — ignored directory
- .env — secret file
- eINVOICE #PE004_2026.xml — invoice/xml data file
- export_project_for_ai.py — exporter script
- keys/ksef_public_key.pem — secret file
- ksef_send_project_for_ai.txt — output file
- project_for_ai.txt — extra ignored file
- public_keys.json — secret file
- safety_send.txt — .gitignore

# FILE CONTENTS


===== FILE START: .env.example =====

KSEF_ENV=test
KSEF_BASE_URL=https://api.ksef.mf.gov.pl/v2
KSEF_NIP=12345678910
KSEF_TOKEN=your_token
KSEF_PUBLIC_KEY_PATH=keys/ksef_public_key.pem
PDF_RENDER_SCRIPT=path/to/render_invoice.mjs

ALLOW_PROD_SEND=false

===== FILE END: .env.example =====


===== FILE START: .gitignore =====

# env
.env

# python
__pycache__/
*.pyc
*.pyo
*.pyd

# keys / secrets
keys/
*.pem
*.key
*.crt

# runtime data
outbound/

public_keys.json
safety_send.txt

# logs
*.log

# system files
.DS_Store
Thumbs.db

# node (jeśli kiedyś będzie PDF renderer)
node_modules/

===== FILE END: .gitignore =====


===== FILE START: auth.py =====

import base64
import time
from pathlib import Path

from cryptography import x509
from cryptography.hazmat.backends import default_backend
from cryptography.hazmat.primitives import hashes
from cryptography.hazmat.primitives.asymmetric import padding

from config import KSEF_BASE_URL, NIP, KSEF_TOKEN, PUBLIC_KEY_PATH, AUTH_POLL_INTERVAL
from utils import save_json
from http_client import HttpClient

class KSeFAuthClient:
    def __init__(self, http: HttpClient, auth_dir):
        self.http = http
        self.auth_dir = auth_dir

    def load_public_key(self):
        cert_bytes = Path(PUBLIC_KEY_PATH).read_bytes()
        cert = x509.load_pem_x509_certificate(cert_bytes, default_backend())
        return cert.public_key()

    def get_challenge(self):
        url = f"{KSEF_BASE_URL}/auth/challenge"
        response = self.http.request("POST", url)
        data = response.json()
        save_json(self.auth_dir / "01_challenge.json", data)
        return data

    def encrypt_token(self, token: str, timestamp_ms: int) -> str:
        payload = f"{token}|{timestamp_ms}".encode("utf-8")
        public_key = self.load_public_key()

        encrypted = public_key.encrypt(
            payload,
            padding.OAEP(
                mgf=padding.MGF1(algorithm=hashes.SHA256()),
                algorithm=hashes.SHA256(),
                label=None,
            ),
        )
        return base64.b64encode(encrypted).decode("ascii")

    def init_auth(self, challenge: str, encrypted_token: str):
        url = f"{KSEF_BASE_URL}/auth/ksef-token"
        body = {
            "challenge": challenge,
            "contextIdentifier": {
                "type": "Nip",
                "value": NIP
            },
            "encryptedToken": encrypted_token
        }
        response = self.http.request("POST", url, json_body=body)
        data = response.json()
        save_json(self.auth_dir / "02_auth_init.json", data)
        return data

    def get_auth_status(self, authentication_token: str, reference_number: str):
        url = f"{KSEF_BASE_URL}/auth/{reference_number}"
        headers = {
            "Authorization": f"Bearer {authentication_token}"
        }
        response = self.http.request("GET", url, headers=headers)
        return response.json()

    def wait_for_auth(self, authentication_token: str, reference_number: str, max_attempts=90):
        for attempt in range(1, max_attempts + 1):
            data = self.get_auth_status(authentication_token, reference_number)
            save_json(self.auth_dir / "03_auth_status.json", data)

            status = data.get("status", {})
            code = status.get("code")
            desc = status.get("description", "")
            print(f"[AUTH] próba={attempt} code={code} desc={desc}")

            if code == 200:
                return data

            if isinstance(code, int) and code >= 400 and code != 429:
                raise RuntimeError(f"Błąd uwierzytelnienia: {data}")

            time.sleep(AUTH_POLL_INTERVAL)

        raise TimeoutError("Przekroczono czas oczekiwania na zakończenie uwierzytelnienia.")

    def redeem(self, authentication_token: str):
        url = f"{KSEF_BASE_URL}/auth/token/redeem"
        headers = {
            "Authorization": f"Bearer {authentication_token}"
        }
        response = self.http.request("POST", url, headers=headers)
        data = response.json()
        save_json(self.auth_dir / "04_redeem.json", data)
        return data

    def authenticate(self):
        challenge_data = self.get_challenge()
        challenge = challenge_data["challenge"]
        timestamp_ms = challenge_data["timestampMs"]

        encrypted_token = self.encrypt_token(KSEF_TOKEN, timestamp_ms)
        auth_init = self.init_auth(challenge, encrypted_token)

        # authentication_token = auth_init["authenticationToken"]
        # reference_number = auth_init["referenceNumber"]
        auth_token_data = auth_init["authenticationToken"]

        if isinstance(auth_token_data, dict):
            authentication_token = auth_token_data["token"]
        else:
            authentication_token = auth_token_data

        reference_number = auth_init["referenceNumber"]
        self.wait_for_auth(authentication_token, reference_number)
        redeem_data = self.redeem(authentication_token)
        return redeem_data

===== FILE END: auth.py =====


===== FILE START: config.py =====

import os
from pathlib import Path
from dotenv import load_dotenv


BASE_DIR = Path(__file__).resolve().parent

load_dotenv(BASE_DIR / ".env")


def resolve_path(value: str | None, default: str) -> Path:
    raw = value or default
    path = Path(raw)

    if path.is_absolute():
        return path

    return BASE_DIR / path


# ------------------------------------------------------------
# KSeF API
# ------------------------------------------------------------

KSEF_ENV = os.getenv("KSEF_ENV", "test").lower()

if KSEF_ENV == "prod":
    KSEF_BASE_URL = os.getenv(
        "KSEF_BASE_URL",
        "https://api.ksef.mf.gov.pl/v2"
    )
elif KSEF_ENV == "demo":
    KSEF_BASE_URL = os.getenv(
        "KSEF_BASE_URL",
        "https://api-demo.ksef.mf.gov.pl/v2"
    )
else:
    KSEF_BASE_URL = os.getenv(
        "KSEF_BASE_URL",
        "https://api-test.ksef.mf.gov.pl/v2"
    )


KSEF_NIP = os.getenv("KSEF_NIP", "")
NIP = KSEF_NIP
FA_SCHEMA_VERSION = os.getenv("FA_SCHEMA_VERSION", "FA(3)")
KSEF_TOKEN = os.getenv("KSEF_TOKEN", "")

PUBLIC_KEY_PATH = resolve_path(
    os.getenv("KSEF_PUBLIC_KEY_PATH"),
    "keys/ksef_public_key.pem"
)

AUTH_POLL_INTERVAL = int(os.getenv("AUTH_POLL_INTERVAL", "2"))

ALLOW_PROD_SEND = os.getenv("ALLOW_PROD_SEND", "false").lower() == "true"

# ------------------------------------------------------------
# Katalogi robocze
# ------------------------------------------------------------

OUTBOUND_DIR = resolve_path(
    os.getenv("KSEF_SEND_OUTBOUND_DIR"),
    "outbound"
)

PENDING_DIR = OUTBOUND_DIR / "pending"
APPROVED_DIR = OUTBOUND_DIR / "approved"
SENT_DIR = OUTBOUND_DIR / "sent"
REJECTED_DIR = OUTBOUND_DIR / "rejected"
LOGS_DIR = OUTBOUND_DIR / "logs"
AUTH_DIR = OUTBOUND_DIR / "auth"
PREVIEW_DIR = OUTBOUND_DIR / "preview"
ARCHIVE_DIR = OUTBOUND_DIR / "archive"
FINAL_PDF_DIR = OUTBOUND_DIR / "final_pdf"
# ------------------------------------------------------------
# Walidacja FA
# ------------------------------------------------------------

FA_XSD_PATH = resolve_path(
    os.getenv("FA_XSD_PATH"),
    "xsd/fa.xsd"
)

PDF_RENDER_SCRIPT = resolve_path(
    os.getenv("PDF_RENDER_SCRIPT"),
    "pdf_generator/render_invoice.mjs"
)

def ensure_dirs():
    for d in (
        OUTBOUND_DIR,
        PENDING_DIR,
        APPROVED_DIR,
        SENT_DIR,
        REJECTED_DIR,
        LOGS_DIR,
        AUTH_DIR,
        PREVIEW_DIR,
        ARCHIVE_DIR,
        FINAL_PDF_DIR,
        
    ):
        d.mkdir(parents=True, exist_ok=True)


def validate_config():
    errors = []

    print("Środowisko KSeF:", KSEF_ENV)
    print("Schemat faktury:", FA_SCHEMA_VERSION)

    if KSEF_ENV == "prod":
        print("UWAGA: skonfigurowano środowisko PRODUKCYJNE.")

    if KSEF_ENV == "prod" and ALLOW_PROD_SEND:
        print("UWAGA: ALLOW_PROD_SEND=true — wysyłka produkcyjna odblokowana.")

    # Ostrzeżenie jeśli ENV=test ale API produkcyjne
    if KSEF_ENV != "prod" and "api.ksef.mf.gov.pl" in KSEF_BASE_URL:
        print("\n⚠ UWAGA: KSEF_ENV nie jest prod, ale używasz API produkcyjnego.")
        print("ALLOW_PROD_SEND=false powinno nadal blokować wysyłkę.\n")

    if KSEF_ENV not in ("test", "demo", "prod"):
        errors.append("KSEF_ENV musi być: test, demo albo prod")

    if not KSEF_BASE_URL:
        errors.append("Brak KSEF_BASE_URL")

    if not KSEF_NIP:
        errors.append("Brak KSEF_NIP")

    if not KSEF_TOKEN:
        errors.append("Brak KSEF_TOKEN")

    if not PUBLIC_KEY_PATH.exists():
        errors.append(f"Brak klucza publicznego KSeF: {PUBLIC_KEY_PATH}")

    if errors:
        raise RuntimeError("Błędy konfiguracji: " + "; ".join(errors))

    if KSEF_ENV == "prod" and not ALLOW_PROD_SEND:
        print("\n⚠ PRODUKCJA KSeF jest zablokowana.")
        print("Aby wysyłać faktury ustaw w .env:")
        print("ALLOW_PROD_SEND=true\n")

===== FILE END: config.py =====


===== FILE START: http_client.py =====

import requests


class HttpClient:
    def __init__(self, base_url: str | None = None):
        self.base_url = base_url.rstrip("/") if base_url else ""
        
    def safe_json_response(response):
        try:
            return response.json()
        except Exception:
            return {
                "_not_json": True,
                "status_code": response.status_code,
                "text": response.text,
            }

    def request(self, method: str, url: str, headers=None, json_body=None):
        response = requests.request(
            method=method,
            url=url,
            headers=headers,
            json=json_body,
            timeout=60,
        )

        return response

    def post(self, path: str, headers=None, json=None):
        url = f"{self.base_url}/{path.lstrip('/')}"
        response = requests.post(
            url,
            headers=headers,
            json=json,
            timeout=60,
        )

        return {
            "status_code": response.status_code,
            "text": response.text,
            "json": self._safe_json(response),
        }

    def get(self, path: str, headers=None):
        url = f"{self.base_url}/{path.lstrip('/')}"
        response = requests.get(
            url,
            headers=headers,
            timeout=60,
        )

        return {
            "status_code": response.status_code,
            "text": response.text,
            "json": self._safe_json(response),
        }

    def _safe_json(self, response):
        try:
            return response.json()
        except Exception:
            return None

===== FILE END: http_client.py =====


===== FILE START: ISSUE1.md =====

🛠 Task / Refactor: Spójność nazw funkcji, przenoszenie XML po akceptacji oraz stan pośredni KSeF
Opis zadania
W module ksef_real_send.py oraz w logice wysyłki i akceptacji faktur występują niespójności i brakujące kroki, które mogą prowadzić do błędów lub ponownej wysyłki tej samej faktury.
Dodatkowo brakuje oznaczenia stanu pośredniego w manifeście, gdy KSeF nie zwróci numeru KSeF.

Uzasadnienie
main.py wywołuje funkcję, która nie istnieje → ryzyko błędu wykonania.

Brak przeniesienia pliku XML z katalogu approved do sent po akceptacji → ryzyko ponownej wysyłki tej samej faktury.

KSeF często zwraca status bez numeru KSeF (asynchroniczne przetwarzanie) → brak stanu pośredniego utrudnia późniejszy polling/status-check.

Zakres prac
1. Spójność nazw funkcji
W ksef_real_send.py istnieje:

python
def send_invoice_real():
Natomiast main.py wywołuje:

python
result = send_approved_real()
Należy:

Opcja A — poprawić import i wywołanie w main.py:

python
from sender.ksef_real_send import send_invoice_real
result = send_invoice_real()
Opcja B — dodać alias w ksef_real_send.py (minimalny patch):

python
def send_approved_real():
    return send_invoice_real()
2. Przenoszenie faktury po akceptacji
Po wywołaniu:

python
mark_invoice_accepted(...)
należy upewnić się, że plik XML opuszcza katalog approved.

Proponowane rozwiązanie:

python
target = config.SENT_DIR / xml_file.name
xml_file.rename(target)
lub:

python
def move_approved_to_sent(xml_file):
    ...
3. Stan pośredni w manifeście
KSeF może zwrócić status bez numeru KSeF. Obecnie jest to blokowane:

python
if not ksef_number:
    result["blocked_count"] += 1
Należy dodać oznaczenie stanu pośredniego, np.:

python
manifest["status"] = "sent_waiting_for_ksef_number"
co umożliwi późniejszy polling/status-check.

Kryteria akceptacji
[ ] Funkcja wywoływana w main.py istnieje lub ma alias.

[ ] Po akceptacji faktury XML jest przenoszony z approved do sent.

[ ] Manifest obsługuje stan sent_waiting_for_ksef_number.

[ ] Kod pozostaje zgodny z konwencją projektu.

Pliki / moduły
main.py

sender/ksef_real_send.py

sender/send_manifest.py (jeśli manifest jest tam aktualizowany)

logika katalogów: approved → sent

===== FILE END: ISSUE1.md =====


===== FILE START: main.py =====

import json
import shutil
import config

from datetime import datetime

from http_client import HttpClient
from auth import KSeFAuthClient
from validator.validate_fa import validate_pending_directory, validate_xml_basic
from validator.business_rules import check_business_rules

from sender.send_manifest import upsert_invoice
from sender.send_invoice import prepare_invoice_payload
from sender.ksef_sender import send_approved_dry_run

from sender.pdf_preview import generate_pdf_preview
from sender.archive import archive_invoice
from sender.ksef_online_session import get_access_token

from sender.ksef_real_send import send_invoice_real


def print_menu():
    print("\nKSeF Send")
    print("1 - sprawdź konfigurację")
    print("2 - waliduj XML w outbound/pending")
    print("3 - przygotuj faktury do wysyłki")
    print("4 - symuluj wysyłkę approved → sent")
    print("5 - przygotuj payload faktury")
    print("6 - test http client")
    print("7 - test autoryzacji KSeF")
    print("8 - REALNA wysyłka do KSeF")
    print("9 - archiwizuj pierwszą fakturę z sent")
    print("10 - wyjście")
    print("11 - pobierz accessToken KSeF")
    print("12 - test zapisu numeru KSeF do manifestu")
    print("13 - Status faktury aż do uzyskania numeru KSeF")
    print("14 - Status manifestu")
    print("15 - Reset testowy")
    print("16 - Pokaż kolejkę wysyłki")
    
def check_config():
    config.ensure_dirs()
    config.validate_config()

    print("Konfiguracja OK")
    print("Środowisko:", config.KSEF_ENV)
    print("API:", config.KSEF_BASE_URL)
    print("Pending:", config.PENDING_DIR)


def validate_pending():
    config.ensure_dirs()

    report = validate_pending_directory(config.PENDING_DIR)

    print("\nRaport walidacji XML")
    print("Katalog:", report["pending_dir"])
    print("Plików XML:", report["count"])
    print("Poprawnych:", report["valid_count"])
    print("Błędnych:", report["invalid_count"])

    for item in report["results"]:
        status = "OK" if item["ok"] else "BŁĄD"
        print(f"- {status}: {item['file']}")

        for warning in item["warnings"]:
            print(f"  OSTRZEŻENIE: {warning}")

        for error in item["errors"]:
            print(f"  BŁĄD: {error}")

    out_file = config.LOGS_DIR / "validate_pending_report.json"
    out_file.write_text(
        json.dumps(report, ensure_ascii=False, indent=2),
        encoding="utf-8"
    )

    print("Raport zapisany:", out_file)


def approve_valid_xml():
    config.ensure_dirs()

    pending = config.PENDING_DIR
    approved = config.APPROVED_DIR

    files = list(pending.glob("*.xml"))

    if not files:
        print("Brak XML w pending.")
        return

    moved = 0

    for f in files:
        xml_report = validate_xml_basic(f)

        if not xml_report["ok"]:
            print("BŁĄD XML:", f.name)
            continue

        business = check_business_rules(f)

        if not business["ok"]:
            print("BŁĄD BIZNESOWY:", f.name)
            for err in business["errors"]:
                print("   ", err)
            continue

        target = approved / f.name
        shutil.move(str(f), str(target))

        pdf_path = generate_pdf_preview(target)
        payload = prepare_invoice_payload(target)

        upsert_invoice(
            filename=target.name,
            status="approved",
            xml_path=str(target),
            pdf_path=str(pdf_path),
            sha256=payload["sha256"],
            size_bytes=payload["size_bytes"],
            note="XML validated, business rules passed, PDF preview generated"
        )

        print("OK:", f.name, "→ approved")
        print("PDF preview:", pdf_path)

        moved += 1

    print("Przeniesiono:", moved)


def prepare_payload_for_first_approved():
    config.ensure_dirs()

    files = list(config.APPROVED_DIR.glob("*.xml"))

    if not files:
        print("Brak XML w approved")
        return

    xml_file = files[0]
    payload = prepare_invoice_payload(xml_file)

    print("\nPayload faktury:")
    print("plik:", payload["filename"])
    print("rozmiar:", payload["size_bytes"], "bajtów")
    print("sha256:", payload["sha256"])

    log_file = config.LOGS_DIR / f"payload_{datetime.now().strftime('%Y%m%d_%H%M%S')}.json"

    safe_payload = {
        "filename": payload["filename"],
        "size_bytes": payload["size_bytes"],
        "sha256": payload["sha256"],
    }

    log_file.write_text(
        json.dumps(safe_payload, ensure_ascii=False, indent=2),
        encoding="utf-8"
    )

    print("Log payload:", log_file)


def test_http_client():
    client = HttpClient(config.KSEF_BASE_URL)
    print("HTTP client OK")
    print("Base URL:", client.base_url)


def test_ksef_auth():
    config.ensure_dirs()

    print("BASE URL:", config.KSEF_BASE_URL)
    print("NIP:", config.NIP)
    print("TOKEN length:", len(config.KSEF_TOKEN or ""))
    print("PUBLIC KEY:", config.PUBLIC_KEY_PATH)
    print("PUBLIC KEY exists:", config.PUBLIC_KEY_PATH.exists())

    http = HttpClient(config.KSEF_BASE_URL)
    auth_client = KSeFAuthClient(http, config.AUTH_DIR)

    result = auth_client.authenticate()

    print("\nAutoryzacja OK")
    print("Access token:", result["accessToken"]["token"][:30] + "...")


def main():
    while True:
        print_menu()
        choice = input("Twój wybór: ").strip()

        try:
            if choice == "1":
                check_config()

            elif choice == "2":
                validate_pending()

            elif choice == "3":
                approve_valid_xml()
            elif choice == "4":
                result = send_approved_dry_run()

                print("\nSymulacja wysyłki")
                print("Środowisko:", result["env"])
                print("Tryb:", result["mode"])
                print("Wysłano testowo:", result["sent_count"])
                print("Zablokowano:", result.get("blocked_count", 0))

                for item in result["files"]:
                    print(
                        "-",
                        item["filename"],
                        "|",
                        item.get("status"),
                        "|",
                        item.get("reason", "")
                    )

                print("Log:", result.get("log_file"))

            elif choice == "5":
                prepare_payload_for_first_approved()

            elif choice == "6":
                test_http_client()

            elif choice == "7":
                test_ksef_auth()
                

            elif choice == "8":
                # REAL SEND !!!

                is_prod_api = "https://api.ksef.mf.gov.pl" in config.KSEF_BASE_URL

                if (config.KSEF_ENV == "prod" or is_prod_api) and not config.ALLOW_PROD_SEND:
                    raise RuntimeError(
                        "Realna wysyłka do produkcyjnego KSeF jest zablokowana. "
                        "Ustaw ALLOW_PROD_SEND=true w .env tylko świadomie."
    )

                result = send_approved_real()

                print("\nRealna wysyłka KSeF")
                print("Środowisko:", result["env"])
                print("Tryb:", result["mode"])
                print("Wysłano:", result["sent_count"])
                print("Zablokowano:", result["blocked_count"])

                for item in result["files"]:
                    print("-", item["filename"], "|", item["status"], "|", item.get("reason", ""))

            elif choice == "9":

                from sender.archive import archive_invoice

                files = list(config.SENT_DIR.glob("*.xml"))

                if not files:
                    print("Brak XML w sent.")
                    continue

                xml_file = files[0]

                pdf_file = config.PREVIEW_DIR / f"{xml_file.stem}.pdf"

                archive_dir = archive_invoice(
                    xml_path=xml_file,
                    pdf_path=pdf_file,
                    response={
                        "status": "manual_archive_test",
                        "note": "Archiwizacja testowa bez realnej wysyłki do KSeF"
                    },
                    metadata={
                        "source_status": "sent",
                        "ksef_number": None
                    }
                )

                print("Zarchiwizowano:", archive_dir)
                    
                    
                    
            elif choice == "11":
                from sender.ksef_online_session import get_access_token

                token = get_access_token()
                print("Access token OK:", token[:30] + "...")
         

            elif choice == "12":
                from sender.send_manifest import mark_invoice_accepted

                files = sorted(config.SENT_DIR.glob("*.xml"))

                if not files:
                    print("Brak XML w sent.")
                    continue

                xml_file = files[0]

                ksef_number = "TEST-KSEF-20260426-ABCDEF123456"
                invoice_ref = "TEST-INVOICE-REF-001"
                session_ref = "TEST-SESSION-REF-001"

                item = mark_invoice_accepted(
                    filename=xml_file.name,
                    ksef_number=ksef_number,
                    invoice_reference=invoice_ref,
                    session_reference=session_ref,
                    response={
                        "status": {
                            "code": 200,
                            "description": "TEST: Faktura przyjęta przez KSeF"
                        },
                        "ksefReferenceNumber": ksef_number
                    }
                )

                print("\nZapisano testowy numer KSeF w manifeście:")
                print(xml_file.name)
                print(item["ksef_number"])
                
            elif choice == "13":

                from sender.ksef_status_poll import poll_ksef_invoice_status
                from sender.send_manifest import load_manifest

                manifest = load_manifest()

                waiting = None

                for item in manifest["items"]:
                    if item.get("status") != "accepted":
                        if item.get("session_reference") and item.get("invoice_reference"):
                            waiting = item
                            break

                if not waiting:
                    print("Brak faktur oczekujących na numer KSeF.")
                    continue

                result = poll_ksef_invoice_status(
                    filename=waiting["filename"],
                    session_reference=waiting["session_reference"],
                    invoice_reference=waiting["invoice_reference"]
                )

                print("\nStatus faktury:")
                print(result)
            
            elif choice == "14":
                from sender.send_manifest import load_manifest

                manifest = load_manifest()

                print("\nMANIFEST FAKTUR\n")

                for item in manifest["items"]:
                    print(
                    item.get("filename"),
                    "|",
                    item.get("status"),
                    "|",
                     item.get("ksef_number"),
                    )
            elif choice == "15":
                import shutil
                from sender.send_manifest import MANIFEST_PATH

                print("\nRESET TESTOWY\n")

                # przenieś approved → pending
                for f in config.APPROVED_DIR.glob("*.xml"):
                    target = config.PENDING_DIR / f.name
                    shutil.move(str(f), str(target))

                # przenieś sent → pending
                for f in config.SENT_DIR.glob("*.xml"):
                    target = config.PENDING_DIR / f.name
                    shutil.move(str(f), str(target))

                # wyczyść manifest
                if MANIFEST_PATH.exists():
                    MANIFEST_PATH.unlink()
                    print("Manifest usunięty.")

                # wyczyść logi
                for log in config.LOGS_DIR.glob("*.json"):
                    log.unlink()

                print("Pipeline zresetowany.")
                
            elif choice == "16":

                print("\nKOLEJKA WYSYŁKI\n")

                pending = list(config.PENDING_DIR.glob("*.xml"))
                approved = list(config.APPROVED_DIR.glob("*.xml"))
                sent = list(config.SENT_DIR.glob("*.xml"))

                print("PENDING:", len(pending))
                for f in pending:
                    print("  ", f.name)

                print("\nAPPROVED:", len(approved))
                for f in approved:
                    print("  ", f.name)

                print("\nSENT:", len(sent))
                for f in sent:
                    print("  ", f.name)    
                
            elif choice == "10":
                print("Koniec.")
                break    

            else:
                print("Nieznana opcja.")

        except Exception as e:
            print("\nWystąpił błąd:")
            print(e)


if __name__ == "__main__":
    main()

===== FILE END: main.py =====


===== FILE START: README.md =====

# ksef-send

Klient wysyłki faktur do KSeF.

Funkcje:

- walidacja XML FA
- business rules
- preview PDF przed wysyłką
- manifest wysyłki
- archiwizacja faktur
- obsługa numeru KSeF
- integracja z API KSeF 2.x / 3.0

Pipeline faktury:

pending → validated → approved → preview → sent → KSeF → archive

===== FILE END: README.md =====


===== FILE START: requirements.txt =====

aiofiles==24.1.0
aiohappyeyeballs==2.6.1
aiohttp==3.12.12
aiosignal==1.3.2
altair==5.5.0
altgraph==0.17.4
annotated-types==0.7.0
antlr4-python3-runtime==4.9.3
anyio==4.9.0
asgiref==3.8.1
aspose-tasks==25.5.0
attrs==25.3.0
autopep8==2.3.1
backoff==2.2.1
bcrypt==4.3.0
beautifulsoup4==4.13.4
blinker==1.9.0
boto3==1.38.45
botocore==1.38.45
build==1.2.2.post1
cachetools==5.5.2
certifi==2025.6.15
cffi==1.17.1
chardet==5.2.0
charset-normalizer==3.4.2
chroma-hnswlib==0.7.3
chromadb==1.0.13
click==8.2.1
cloudpickle==3.1.1
colorama==0.4.6
colorclass==2.2.2
coloredlogs==15.0.1
compressed-rtf==1.0.7
contourpy==1.3.1
cryptography==45.0.4
cycler==0.12.1
dataclasses-json==0.6.7
defusedxml==0.7.1
Deprecated==1.2.18
dill==0.4.0
distro==1.9.0
docker==7.1.0
durationpy==0.10
easygui==0.98.3
ebcdic==1.1.1
emoji==2.14.1
et_xmlfile==2.0.0
extract-msg==0.54.1
faiss-cpu==1.11.0
fastapi==0.115.14
filelock==3.18.0
filetype==1.2.0
Flask==3.1.0
flatbuffers==25.2.10
flutter==0.1
fonttools==4.55.3
fpdf==1.7.2
fpdf2==2.8.3
frozenlist==1.7.0
fsspec==2025.5.1
git-filter-repo==2.47.0
gitdb==4.0.12
GitPython==3.1.44
google-auth==2.40.3
google-pasta==0.2.0
googleapis-common-protos==1.70.0
graphene==3.4.3
graphql-core==3.2.6
graphql-relay==3.2.0
greenlet==3.2.3
grpcio==1.73.1
h11==0.16.0
hf-xet==1.1.5
html5lib==1.1
httpcore==1.0.9
httptools==0.6.4
httpx==0.28.1
httpx-sse==0.4.0
huggingface-hub==0.33.1
humanfriendly==10.0
idna==3.10
importlib-metadata==6.11.0
importlib_resources==6.5.2
itsdangerous==2.2.0
Jinja2==3.1.5
jiter==0.10.0
jmespath==1.0.1
joblib==1.5.1
jsonpatch==1.33
jsonpointer==3.0.0
jsonschema==4.24.0
jsonschema-specifications==2025.4.1
kiwisolver==1.4.8
kubernetes==33.1.0
langchain==0.3.26
langchain-community==0.3.26
langchain-core==0.3.66
langchain-huggingface==0.3.0
langchain-openai==0.3.27
langchain-text-splitters==0.3.8
langdetect==1.0.9
langsmith==0.4.4
language_tool_python==2.8.1
lark==1.1.9
libpff-python==20231205
lxml==5.3.0
Markdown==3.8.2
markdown-it-py==3.0.0
MarkupSafe==3.0.2
marshmallow==3.26.1
matplotlib==3.10.0
mdurl==0.1.2
mmh3==5.1.0
mock==4.0.3
mpmath==1.3.0
msoffcrypto-tool==5.4.2
multidict==6.4.4
multiprocess==0.70.18
mypy==1.14.1
mypy-extensions==1.0.0
nano==0.10.0
narwhals==1.42.0
nest-asyncio==1.6.0
networkx==3.5
nltk==3.9.1
numpy==1.26.4
oauthlib==3.3.1
olefile==0.47
oletools==0.60.2
omegaconf==2.3.0
onnxruntime==1.22.0
openai==1.97.1
opencv-contrib-python==4.10.0.84
opencv-python==4.10.0.84
openpyxl==3.1.5
opentelemetry-api==1.34.1
opentelemetry-exporter-otlp-proto-common==1.34.1
opentelemetry-exporter-otlp-proto-grpc==1.34.1
opentelemetry-instrumentation==0.55b1
opentelemetry-instrumentation-asgi==0.55b1
opentelemetry-instrumentation-fastapi==0.55b1
opentelemetry-proto==1.34.1
opentelemetry-sdk==1.34.1
opentelemetry-semantic-conventions==0.55b1
opentelemetry-util-http==0.55b1
orjson==3.10.18
outcome==1.3.0.post0
overrides==7.7.0
packaging==23.2
pandas==2.2.3
pathos==0.3.4
pcodedmp==1.2.6
pdf2image==1.17.0
pefile==2023.2.7
pi_heif==0.22.0
pikepdf==9.9.0
pillow==11.0.0
platformdirs==4.3.8
posthog==6.0.0
pox==0.3.6
ppft==1.7.7
propcache==0.3.2
proto-plus==1.26.1
protobuf==5.29.5
psutil==6.1.1
pulsar-client==3.7.0
pyarrow==20.0.0
pyasn1==0.6.1
pyasn1_modules==0.4.2
pybase64==1.4.1
pycocotools==2.0.10
pycodestyle==2.12.1
pycparser==2.22
pydantic==2.11.7
pydantic-settings==2.9.1
pydantic_core==2.33.2
pydeck==0.9.1
Pygments==2.19.2
pyinstaller==6.14.1
pyinstaller-hooks-contrib==2025.5
PyMuPDF==1.25.1
pypandoc==1.15
pyparsing==3.2.0
pypdf==5.6.0
PyPDF2==3.0.1
pypdfium2==4.30.1
PyPika==0.48.9
pyproject_hooks==1.2.0
pyreadline3==3.5.4
PySocks==1.7.1
pytesseract==0.3.13
python-dateutil==2.9.0.post0
python-docx==1.1.2
python-dotenv==1.1.1
python-iso639==2025.2.18
python-magic==0.4.27
python-magic-bin==0.4.14
python-multipart==0.0.20
python-oxmsg==0.0.2
python-pptx==1.0.2
pytz==2024.2
PyWavelets==1.8.0
pywin32==306
pywin32-ctypes==0.2.3
PyYAML==6.0.2
qrcode==8.2
RapidFuzz==3.13.0
red-black-tree-mod==1.22
referencing==0.36.2
regex==2024.11.6
reportlab==4.4.10
requests==2.32.4
requests-oauthlib==2.0.0
requests-toolbelt==1.0.0
rich==14.0.0
rpds-py==0.25.1
rsa==4.9.1
RTFDE==0.1.2.1
s3transfer==0.13.0
safetensors==0.5.3
sagemaker==2.247.1
sagemaker-core==1.0.40
schema==0.7.7
scikit-learn==1.7.0
scipy==1.16.0
selenium==4.34.2
sentence-transformers==4.1.0
shellingham==1.5.4
six==1.17.0
smdebug-rulesconfig==1.0.1
smmap==5.0.2
sniffio==1.3.1
sortedcontainers==2.4.0
soupsieve==2.6
SQLAlchemy==2.0.41
starlette==0.46.2
streamlit==1.45.1
streamlit-chat==0.1.1
sympy==1.14.0
tblib==3.1.0
tenacity==8.5.0
threadpoolctl==3.6.0
tiktoken==0.9.0
tokenizers==0.21.2
toml==0.10.2
torch==2.7.1
tornado==6.5.1
tqdm==4.67.1
transformers==4.53.0
trio==0.30.0
trio-websocket==0.12.2
typer==0.16.0
types-PyYAML==6.0.12.20241230
typing-inspect==0.9.0
typing-inspection==0.4.1
typing_extensions==4.14.0
tzdata==2025.1
tzlocal==5.3.1
unstructured==0.18.1
unstructured-client==0.37.2
unstructured.pytesseract==0.3.15
urllib3==2.5.0
uvicorn==0.34.3
watchdog==6.0.0
watchfiles==1.1.0
webencodings==0.5.1
websocket-client==1.8.0
websockets==15.0.1
Werkzeug==3.1.3
win_unicode_console==0.5
wrapt==1.17.2
wsproto==1.2.0
xlrd==2.0.2
xlsxwriter==3.2.5
xlwings==0.32.1
yarl==1.20.1
zipp==3.23.0
zstandard==0.23.0

===== FILE END: requirements.txt =====


===== FILE START: send-ksef.py =====

import config
from main import validate_pending, approve_valid_xml
from sender.ksef_sender import send_approved_dry_run


def main():
    print("KSeF Send pipeline")

    config.ensure_dirs()
    config.validate_config()

    print("\n1. Walidacja XML...")
    validate_pending()

    print("\n2. Przygotowanie faktur...")
    approve_valid_xml()

    print("\n3. Dry-run wysyłki...")
    result = send_approved_dry_run()

    print("Tryb:", result["mode"])
    print("Wysłano testowo:", result["sent_count"])


if __name__ == "__main__":
    main()

===== FILE END: send-ksef.py =====


===== FILE START: sender/archive.py =====

import json
import shutil
from pathlib import Path
from datetime import datetime, timezone

import config


def utc_now():
    return datetime.now(timezone.utc).isoformat()


def archive_invoice(
    xml_path: Path,
    pdf_path: Path | None = None,
    response: dict | None = None,
    metadata: dict | None = None,
) -> Path:
    xml_path = Path(xml_path)

    if not xml_path.exists():
        raise FileNotFoundError(f"Brak XML do archiwizacji: {xml_path}")

    now = datetime.now(timezone.utc)
    year = now.strftime("%Y")
    month = now.strftime("%m")

    invoice_dir = config.ARCHIVE_DIR / year / month / xml_path.stem
    invoice_dir.mkdir(parents=True, exist_ok=True)

    archived_xml = invoice_dir / "invoice.xml"
    shutil.copy2(xml_path, archived_xml)

    archived_pdf = None
    if pdf_path:
        pdf_path = Path(pdf_path)
        if pdf_path.exists():
            archived_pdf = invoice_dir / "preview.pdf"
            shutil.copy2(pdf_path, archived_pdf)

    if response is None:
        response = {}

    if metadata is None:
        metadata = {}

    metadata_out = {
        "filename": xml_path.name,
        "archived_at": utc_now(),
        "env": config.KSEF_ENV,
        "xml_path": str(archived_xml),
        "pdf_path": str(archived_pdf) if archived_pdf else None,
        **metadata,
    }

    response_file = invoice_dir / "response.json"
    response_file.write_text(
        json.dumps(response, ensure_ascii=False, indent=2),
        encoding="utf-8"
    )

    metadata_file = invoice_dir / "metadata.json"
    metadata_file.write_text(
        json.dumps(metadata_out, ensure_ascii=False, indent=2),
        encoding="utf-8"
    )

    return invoice_dir

===== FILE END: sender/archive.py =====


===== FILE START: sender/email_package.py =====

import json
import shutil
from pathlib import Path

import config
from sender.qr_code import build_ksef_qr_payload
from sender.invoice_summary import read_invoice_summary

INSTRUCTION_FILE = Path(r"C:\Users\dpolz\Documents\Faktura\SETUP\mPOZ2.txt")

def load_instruction_text() -> str:
    if not INSTRUCTION_FILE.exists():
        return ""

    for enc in ("utf-8", "cp1250", "windows-1250"):
        try:
            return INSTRUCTION_FILE.read_text(encoding=enc).strip()
        except UnicodeDecodeError:
            pass

    return INSTRUCTION_FILE.read_text(errors="ignore").strip()

#return INSTRUCTION_FILE.read_text(encoding="cp1250").strip()

def create_email_package(
    filename: str,
    ksef_number: str,
    amount: str | None = None,
):

    stem = Path(filename).stem
    xml_path = config.SENT_DIR / filename
    summary = read_invoice_summary(xml_path)
    package_dir = config.OUTBOUND_DIR / "email" / stem
    package_dir.mkdir(parents=True, exist_ok=True)

    preview_pdf = config.PREVIEW_DIR / f"{stem}.pdf"
    final_pdf = config.FINAL_PDF_DIR / f"{stem}_KSeF.pdf"
    qr_png = config.FINAL_PDF_DIR / f"{stem}_KSeF_QR.png"

    if preview_pdf.exists():
        shutil.copy2(preview_pdf, package_dir / "invoice.pdf")

    if final_pdf.exists():
        shutil.copy2(final_pdf, package_dir / "invoice_KSeF.pdf")

    if qr_png.exists():
        shutil.copy2(qr_png, package_dir / "qr.png")

    verification_link = build_ksef_qr_payload(ksef_number)
    instruction_text = load_instruction_text()
    
    email_text = f"""Dzień dobry,

    przesyłamy fakturę w formacie PDF.

    Dane faktury:
    - Numer faktury: {summary.get("invoice_number", filename)}
    - Data wystawienia: {summary.get("issue_date", "")}
    - Nabywca: {summary.get("buyer_name", "")}
    - Kwota brutto: {summary.get("gross_amount", "")} {summary.get("currency", "PLN")}
    - Numer KSeF: {ksef_number}
    - Link weryfikacyjny KSeF: {verification_link}

    {instruction_text}

    Faktura została wystawiona w Krajowym Systemie e-Faktur.
    Kod QR załączony do pakietu umożliwia weryfikację dokumentu w KSeF.

    W załączeniu:
    - invoice_KSeF.pdf
    - qr.png

    Pozdrawiam
    Dariusz Polzer
    """

    (package_dir / "email.txt").write_text(email_text, encoding="utf-8")

    metadata = {
        "invoice": filename,
        "ksef_number": ksef_number,
    }

    (package_dir / "metadata.json").write_text(
        json.dumps(metadata, indent=2, ensure_ascii=False),
        encoding="utf-8"
    )

    return package_dir

===== FILE END: sender/email_package.py =====


===== FILE START: sender/final_pdf.py =====

import shutil
from pathlib import Path

import config
from sender.qr_code import generate_ksef_qr

def create_final_pdf_with_ksef_number(xml_filename: str, ksef_number: str) -> Path:
    """
    Na razie wersja bez edycji treści PDF:
    kopiuje preview PDF jako final PDF.
    Później dodamy nadruk numeru KSeF / QR.
    """

    stem = Path(xml_filename).stem

    preview_pdf = config.PREVIEW_DIR / f"{stem}.pdf"
    final_pdf = config.FINAL_PDF_DIR / f"{stem}_KSeF.pdf"

    if not preview_pdf.exists():
        raise FileNotFoundError(f"Brak PDF preview: {preview_pdf}")

    shutil.copy2(preview_pdf, final_pdf)

    metadata_file = config.FINAL_PDF_DIR / f"{stem}_KSeF.txt"
    metadata_file.write_text(
        f"Numer KSeF: {ksef_number}\n",
        encoding="utf-8"
    )
    qr_path = generate_ksef_qr(
        ksef_number=ksef_number,
        xml_filename=xml_filename,
    )
    return final_pdf

===== FILE END: sender/final_pdf.py =====


===== FILE START: sender/invoice_summary.py =====

from pathlib import Path
import xml.etree.ElementTree as ET


def _text(root, local_name: str) -> str | None:
    for el in root.iter():
        tag = el.tag.split("}")[-1]
        if tag == local_name and el.text:
            return el.text.strip()
    return None


def read_invoice_summary(xml_path: Path) -> dict:
    xml_path = Path(xml_path)

    if not xml_path.exists():
        return {}

    root = ET.parse(xml_path).getroot()

    summary = {
        "invoice_number": _text(root, "P_2") or _text(root, "NrFaktury"),
        "issue_date": _text(root, "P_1") or _text(root, "DataWystawienia"),
        "seller_nip": _text(root, "NIP"),
        "buyer_name": _text(root, "Nazwa"),
        "gross_amount": (
            _text(root, "P_15")
            or _text(root, "FaWartoscBrutto")
            or _text(root, "Brutto")
        ),
        "currency": _text(root, "KodWaluty") or "PLN",
    }

    return {k: v for k, v in summary.items() if v}

===== FILE END: sender/invoice_summary.py =====


===== FILE START: sender/ksef_online_session.py =====

import json
import time
from pathlib import Path

import config
from auth import KSeFAuthClient
from http_client import HttpClient
from sender.send_invoice import prepare_invoice_payload
from sender.ksef_sender import require_pdf_preview, require_prod_confirmation
from sender.send_manifest import upsert_invoice
from sender.archive import archive_invoice


def get_access_token() -> str:
    config.ensure_dirs()

    http = HttpClient(config.KSEF_BASE_URL)
    auth_client = KSeFAuthClient(http, config.AUTH_DIR)

    result = auth_client.authenticate()
    return result["accessToken"]["token"]


def open_online_session(access_token: str) -> dict:
    """
    TODO: dopasować body do aktualnego OpenAPI KSeF 2.0.
    Na razie funkcja celowo nie jest używana w produkcyjnej wysyłce.
    """

    http = HttpClient(config.KSEF_BASE_URL)

    headers = {
        "Authorization": f"Bearer {access_token}",
        "Content-Type": "application/json",
    }

    body = {
        "formCode": {
            "systemCode": "FA (3)",
            "schemaVersion": "1-0E",
            "value": "FA"
        }
    }

    response = http.request(
        "POST",
        f"{config.KSEF_BASE_URL}/sessions/online",
        headers=headers,
        json_body=body,
    )

    data = response.json()

    if response.status_code >= 400:
        raise RuntimeError(f"Błąd otwarcia sesji online: {data}")

    return data


def send_invoice_in_session(
    access_token: str,
    session_reference: str,
    xml_file: Path,
) -> dict:
    """
    TODO: dopasować finalne body do aktualnego OpenAPI.
    """

    payload = prepare_invoice_payload(xml_file)

    http = HttpClient(config.KSEF_BASE_URL)

    headers = {
        "Authorization": f"Bearer {access_token}",
        "Content-Type": "application/json",
    }

    body = {
        "invoiceHash": payload["sha256"],
        "invoiceSize": payload["size_bytes"],
        "invoicePayload": {
            "type": "base64",
            "invoiceBody": payload["invoice_base64"],
        },
    }

    response = http.request(
        "POST",
        f"{config.KSEF_BASE_URL}/sessions/online/{session_reference}/invoices",
        headers=headers,
        json_body=body,
    )

    data = response.json()

    if response.status_code >= 400:
        raise RuntimeError(f"Błąd wysyłki faktury: {data}")

    return data


def wait_for_invoice_status(
    access_token: str,
    session_reference: str,
    invoice_reference: str,
    max_attempts: int = 60,
    sleep_seconds: int = 5,
) -> dict:
    http = HttpClient(config.KSEF_BASE_URL)

    headers = {
        "Authorization": f"Bearer {access_token}",
    }

    for attempt in range(1, max_attempts + 1):
        response = http.request(
            "GET",
            f"{config.KSEF_BASE_URL}/sessions/{session_reference}/invoices/{invoice_reference}",
            headers=headers,
        )

        data = response.json()

        status = data.get("status", {})
        code = status.get("code")
        desc = status.get("description", "")

        print(f"[INVOICE STATUS] próba={attempt} code={code} desc={desc}")

        if code == 200:
            return data

        if isinstance(code, int) and code >= 400 and code != 429:
            raise RuntimeError(f"Faktura odrzucona: {data}")

        time.sleep(sleep_seconds)

    raise TimeoutError("Przekroczono czas oczekiwania na status faktury.")


def close_online_session(access_token: str, session_reference: str) -> dict:
    http = HttpClient(config.KSEF_BASE_URL)

    headers = {
        "Authorization": f"Bearer {access_token}",
    }

    response = http.request(
        "POST",
        f"{config.KSEF_BASE_URL}/sessions/online/{session_reference}/close",
        headers=headers,
    )

    data = response.json()

    if response.status_code >= 400:
        raise RuntimeError(f"Błąd zamykania sesji online: {data}")

    return data


def send_first_approved_invoice_real() -> dict:
    """
    Bezpieczny real-send scaffold:
    - wymaga PDF preview,
    - wymaga blokady/potwierdzenia PROD,
    - na razie NIE wywołuj tej funkcji bez sprawdzenia body z OpenAPI.
    """

    files = sorted(config.APPROVED_DIR.glob("*.xml"))

    if not files:
        return {
            "ok": False,
            "message": "Brak XML w approved.",
        }

    xml_file = files[0]
    pdf_file = config.PREVIEW_DIR / f"{xml_file.stem}.pdf"

    require_pdf_preview(xml_file)
    require_prod_confirmation(xml_file.name)

    raise RuntimeError(
        "Realna wysyłka jest celowo zatrzymana. "
        "Najpierw dopasuj body open_online_session() i send_invoice_in_session() "
        "do aktualnego OpenAPI KSeF 2.0."
    )

===== FILE END: sender/ksef_online_session.py =====


===== FILE START: sender/ksef_real_send.py =====

import shutil

import config
from auth import KSeFAuthClient
from http_client import HttpClient

from sender.send_invoice import prepare_invoice_payload
from sender.session_ledger import log_event
from sender.send_manifest import mark_invoice_accepted, upsert_invoice
from sender.ksef_sender import require_pdf_preview, require_prod_confirmation


def send_invoice_real():
    config.ensure_dirs()

    files = sorted(config.APPROVED_DIR.glob("*.xml"))

    result = {
        "mode": "real_send",
        "env": config.KSEF_ENV,
        "sent_count": 0,
        "blocked_count": 0,
        "files": []
    }

    if not files:
        return result

    xml_file = files[0]

    try:
        require_pdf_preview(xml_file)
        require_prod_confirmation(xml_file.name)

        http = HttpClient(config.KSEF_BASE_URL)
        auth = KSeFAuthClient(http, config.AUTH_DIR)

        auth_result = auth.authenticate()
        access_token = auth_result["accessToken"]["token"]

        log_event(
            event_type="auth",
            status="ok",
            filename=xml_file.name,
        )

        headers = {
            "Authorization": f"Bearer {access_token}",
            "Content-Type": "application/json"
        }

        session_response = http.request(
            "POST",
            f"{config.KSEF_BASE_URL}/sessions/online",
            headers=headers,
            json_body={}
        )

        session = session_response.json()
        session_ref = session["referenceNumber"]

        log_event(
            event_type="open_session",
            status="ok",
            filename=xml_file.name,
            session_reference=session_ref,
            response=session
        )

        payload = prepare_invoice_payload(xml_file)

        invoice_response = http.request(
            "POST",
            f"{config.KSEF_BASE_URL}/sessions/online/{session_ref}/invoices",
            headers=headers,
            json_body=payload
        )

        invoice = invoice_response.json()
        invoice_ref = invoice["referenceNumber"]

        log_event(
            event_type="send_invoice",
            status="ok",
            filename=xml_file.name,
            session_reference=session_ref,
            invoice_reference=invoice_ref,
            response=invoice
        )

        # Zapis stanu pośredniego — potrzebne do późniejszego pollingu,
        # jeśli KSeF nie zwróci numeru od razu.
        upsert_invoice(
            filename=xml_file.name,
            status="waiting_for_ksef_number",
            xml_path=str(xml_file),
            invoice_reference=invoice_ref,
            session_reference=session_ref,
            response=invoice,
            note="Faktura wysłana do KSeF, oczekuje na numer KSeF"
        )

        status_response = http.request(
            "GET",
            f"{config.KSEF_BASE_URL}/sessions/online/{session_ref}/invoices/{invoice_ref}",
            headers=headers
        )

        status = status_response.json()
        ksef_number = status.get("ksefReferenceNumber")

        log_event(
            event_type="invoice_status",
            status="ok",
            filename=xml_file.name,
            session_reference=session_ref,
            invoice_reference=invoice_ref,
            ksef_number=ksef_number,
            response=status
        )

        if not ksef_number:
            result["blocked_count"] += 1
            result["files"].append({
                "filename": xml_file.name,
                "status": "waiting_for_ksef_number",
                "session_reference": session_ref,
                "invoice_reference": invoice_ref,
                "response": status,
            })
            return result

        target = config.SENT_DIR / xml_file.name

        if target.exists():
            raise RuntimeError(
                f"Plik już istnieje w sent: {target}. "
                "Przerwano, aby nie nadpisać pliku."
            )

        shutil.move(str(xml_file), str(target))

        mark_invoice_accepted(
            filename=target.name,
            ksef_number=ksef_number,
            invoice_reference=invoice_ref,
            session_reference=session_ref,
            response=status,
        )

        result["files"].append({
            "filename": target.name,
            "status": "accepted",
            "ksef_number": ksef_number
        })

        result["sent_count"] = 1
        return result

    except Exception as e:
        log_event(
            event_type="error",
            status="failed",
            filename=xml_file.name,
            error=str(e)
        )
        raise


def send_approved_real():
    return send_invoice_real()

# import config
# import shutil

# from auth import KSeFAuthClient
# from http_client import HttpClient

# from sender.send_invoice import prepare_invoice_payload
# from sender.session_ledger import log_event
# from sender.send_manifest import mark_invoice_accepted
# from sender.ksef_sender import require_pdf_preview, require_prod_confirmation


# def send_invoice_real():
    # config.ensure_dirs()

    # files = sorted(config.APPROVED_DIR.glob("*.xml"))

    # result = {
        # "mode": "real_send",
        # "env": config.KSEF_ENV,
        # "sent_count": 0,
        # "blocked_count": 0,
        # "files": []
    # }

    # if not files:
        # return result

    # xml_file = files[0]

    # try:

        # # ------------------------------------------------------------
        # # Zabezpieczenia
        # # ------------------------------------------------------------

        # require_pdf_preview(xml_file)
        # require_prod_confirmation(xml_file.name)

        # # ------------------------------------------------------------
        # # Autoryzacja
        # # ------------------------------------------------------------

        # http = HttpClient(config.KSEF_BASE_URL)
        # auth = KSeFAuthClient(http, config.AUTH_DIR)

        # auth_result = auth.authenticate()
        # access_token = auth_result["accessToken"]["token"]

        # log_event(
            # event_type="auth",
            # status="ok",
            # filename=xml_file.name,
        # )

        # headers = {
            # "Authorization": f"Bearer {access_token}",
            # "Content-Type": "application/json"
        # }

        # # ------------------------------------------------------------
        # # Otwarcie sesji KSeF
        # # ------------------------------------------------------------

        # session_response = http.request(
            # "POST",
            # f"{config.KSEF_BASE_URL}/sessions/online",
            # headers=headers,
            # json_body={}
        # )

        # session = session_response.json()
        # session_ref = session["referenceNumber"]

        # log_event(
            # event_type="open_session",
            # status="ok",
            # filename=xml_file.name,
            # session_reference=session_ref,
            # response=session
        # )

        # # ------------------------------------------------------------
        # # Przygotowanie payloadu faktury
        # # ------------------------------------------------------------

        # payload = prepare_invoice_payload(xml_file)

        # invoice_response = http.request(
            # "POST",
            # f"{config.KSEF_BASE_URL}/sessions/online/{session_ref}/invoices",
            # headers=headers,
            # json_body=payload
        # )

        # invoice = invoice_response.json()
        # invoice_ref = invoice["referenceNumber"]

        # log_event(
            # event_type="send_invoice",
            # status="ok",
            # filename=xml_file.name,
            # session_reference=session_ref,
            # invoice_reference=invoice_ref,
            # response=invoice
        # )

        # # ------------------------------------------------------------
        # # Sprawdzenie statusu faktury
        # # ------------------------------------------------------------

        # status_response = http.request(
            # "GET",
            # f"{config.KSEF_BASE_URL}/sessions/online/{session_ref}/invoices/{invoice_ref}",
            # headers=headers
        # )

        # status = status_response.json()
        # ksef_number = status.get("ksefReferenceNumber")

        # log_event(
            # event_type="invoice_status",
            # status="ok",
            # filename=xml_file.name,
            # session_reference=session_ref,
            # invoice_reference=invoice_ref,
            # ksef_number=ksef_number,
            # response=status
        # )

        # # ------------------------------------------------------------
        # # Jeśli brak numeru KSeF – blokujemy
        # # ------------------------------------------------------------

        # if not ksef_number:
            # result["blocked_count"] += 1
            # result["files"].append({
                # "filename": xml_file.name,
                # "status": "status_without_ksef_number",
                # "response": status,
            # })
            # return result

        # # ------------------------------------------------------------
        # # Przeniesienie XML do SENT
        # # ------------------------------------------------------------

        # target = config.SENT_DIR / xml_file.name
        # shutil.move(str(xml_file), str(target))

        # # ------------------------------------------------------------
        # # Aktualizacja manifestu
        # # ------------------------------------------------------------

        # mark_invoice_accepted(
            # filename=target.name,
            # ksef_number=ksef_number,
            # invoice_reference=invoice_ref,
            # session_reference=session_ref,
            # response=status,
        # )

        # result["files"].append({
            # "filename": target.name,
            # "status": "accepted",
            # "ksef_number": ksef_number
        # })

        # result["sent_count"] = 1

        # return result

    # except Exception as e:

        # log_event(
            # event_type="error",
            # status="failed",
            # filename=xml_file.name,
            # error=str(e)
        # )

        # raise

===== FILE END: sender/ksef_real_send.py =====


===== FILE START: sender/ksef_sender.py =====

import json
import shutil
from pathlib import Path

import config
from utils import utc_now
from sender.send_invoice import prepare_invoice_payload
from sender.send_manifest import upsert_invoice


def require_pdf_preview(xml_file: Path):
    pdf_file = config.PREVIEW_DIR / f"{xml_file.stem}.pdf"

    if not pdf_file.exists():
        raise RuntimeError(
            f"Brak podglądu PDF dla faktury: {xml_file.name}. "
            f"Najpierw wygeneruj PDF: {pdf_file}"
        )

    print("\nPodgląd PDF istnieje:")
    print(pdf_file)

    confirm = input("Czy sprawdziłeś PDF faktury? Wpisz: SPRAWDZIŁEM PDF: ").strip()

    if confirm != "SPRAWDZIŁEM PDF":
        raise RuntimeError("Przerwano wysyłkę — PDF nie został potwierdzony.")


def require_prod_confirmation(invoice_filename: str):
    if config.KSEF_ENV != "prod":
        return

    if not config.ALLOW_PROD_SEND:
        raise RuntimeError(
            "Wysyłka do PRODUKCJI jest zablokowana. "
            "Ustaw ALLOW_PROD_SEND=true tylko świadomie."
        )

    print("\nUWAGA: ŚRODOWISKO PRODUKCYJNE KSeF")
    print("Faktura:", invoice_filename)
    print("To może mieć skutki podatkowe.")

    confirm = input("Aby wysłać wpisz: WYSYŁAM DO KSEF PROD: ").strip()

    if confirm != "WYSYŁAM DO KSEF PROD":
        raise RuntimeError("Przerwano wysyłkę produkcyjną.")


def send_approved_dry_run() -> dict:
    """
    Tryb testowy bez wysyłki do KSeF.
    Przenosi XML z approved do sent, ale wcześniej sprawdza manifest,
    żeby nie ruszyć faktury już zaakceptowanej w KSeF.
    """

    config.ensure_dirs()

    files = sorted(config.APPROVED_DIR.glob("*.xml"))

    result = {
        "mode": "dry_run",
        "env": config.KSEF_ENV,
        "sent_count": 0,
        "blocked_count": 0,
        "files": [],
        "created_at": utc_now(),
    }

    if not files:
        return result

    for xml_file in files:
        try:
            payload = prepare_invoice_payload(xml_file)

            upsert_invoice(
                filename=xml_file.name,
                status="dry_run_check",
                xml_path=str(xml_file),
                sha256=payload["sha256"],
                size_bytes=payload["size_bytes"],
                note="Dry run duplicate check before moving to sent"
            )

            target = config.SENT_DIR / xml_file.name

            if target.exists():
                raise RuntimeError(
                    f"Plik już istnieje w sent: {target}. "
                    "Przerwano dry run, aby nie nadpisać pliku."
                )

            shutil.move(str(xml_file), str(target))

            upsert_invoice(
                filename=target.name,
                status="dry_run_sent",
                xml_path=str(target),
                sha256=payload["sha256"],
                size_bytes=payload["size_bytes"],
                note="Dry run moved approved invoice to sent"
            )

            result["files"].append({
                "filename": target.name,
                "source": str(xml_file),
                "target": str(target),
                "status": "dry_run_sent",
                "sent_at": utc_now(),
            })

            result["sent_count"] += 1

        except Exception as e:
            result["blocked_count"] += 1
            result["files"].append({
                "filename": xml_file.name,
                "status": "blocked",
                "reason": str(e),
            })

    log_file = config.LOGS_DIR / f"send_dry_run_{utc_now().replace(':', '').replace('-', '')}.json"
    log_file.write_text(
        json.dumps(result, ensure_ascii=False, indent=2),
        encoding="utf-8"
    )

    result["log_file"] = str(log_file)
    return result

===== FILE END: sender/ksef_sender.py =====


===== FILE START: sender/ksef_status_poll.py =====

import time
import config

from auth import KSeFAuthClient
from http_client import HttpClient
from sender.session_ledger import log_event
from sender.send_manifest import mark_invoice_accepted


def poll_ksef_invoice_status(
    filename: str,
    session_reference: str,
    invoice_reference: str,
    max_attempts: int = 10,
    delay_seconds: int = 5,
) -> dict:
    """
    Sprawdza status faktury w KSeF aż do uzyskania numeru KSeF
    albo wyczerpania limitu prób.
    """

    config.ensure_dirs()

    http = HttpClient(config.KSEF_BASE_URL)
    auth = KSeFAuthClient(http, config.AUTH_DIR)

    auth_result = auth.authenticate()
    access_token = auth_result["accessToken"]["token"]

    headers = {
        "Authorization": f"Bearer {access_token}",
        "Content-Type": "application/json"
    }

    for attempt in range(1, max_attempts + 1):
        status_response = http.request(
            "GET",
            f"{config.KSEF_BASE_URL}/sessions/online/{session_reference}/invoices/{invoice_reference}",
            headers=headers
        )

        status = status_response.json()
        ksef_number = status.get("ksefReferenceNumber")

        log_event(
            event_type="poll_invoice_status",
            status="ok",
            filename=filename,
            session_reference=session_reference,
            invoice_reference=invoice_reference,
            ksef_number=ksef_number,
            response=status,
        )

        if ksef_number:
            mark_invoice_accepted(
                filename=filename,
                ksef_number=ksef_number,
                invoice_reference=invoice_reference,
                session_reference=session_reference,
                response=status,
            )

            return {
                "filename": filename,
                "status": "accepted",
                "ksef_number": ksef_number,
                "attempts": attempt,
                "response": status,
            }

        time.sleep(delay_seconds)

    return {
        "filename": filename,
        "status": "waiting_timeout",
        "ksef_number": None,
        "attempts": max_attempts,
    }

===== FILE END: sender/ksef_status_poll.py =====


===== FILE START: sender/load_invoice.py =====

from pathlib import Path


def load_invoice_xml(xml_path: Path) -> str:
    xml_path = Path(xml_path)

    if not xml_path.exists():
        raise FileNotFoundError(xml_path)

    return xml_path.read_text(encoding="utf-8")

===== FILE END: sender/load_invoice.py =====


===== FILE START: sender/pdf_preview.py =====

import subprocess
from pathlib import Path

import config


def generate_pdf_preview(xml_path: Path) -> Path:
    xml_path = Path(xml_path).resolve()

    config.ensure_dirs()

    pdf_path = (config.PREVIEW_DIR / f"{xml_path.stem}.pdf").resolve()

    if pdf_path.exists():
        return pdf_path

    cmd = [
        "node",
        str(config.PDF_RENDER_SCRIPT),
        str(xml_path),
        str(pdf_path),
    ]

    subprocess.run(
        cmd,
        check=True,
        cwd=str(config.BASE_DIR),
    )

    if not pdf_path.exists():
        raise RuntimeError(f"PDF nie został utworzony: {pdf_path}")

    return pdf_path

===== FILE END: sender/pdf_preview.py =====


===== FILE START: sender/qr_code.py =====

from pathlib import Path
import qrcode

import config


def build_ksef_qr_payload(ksef_number: str) -> str:
    """
    Tymczasowy payload QR.
    Docelowy format linku należy dopasować do aktualnej specyfikacji MF.
    """
    return f"KSeF: {ksef_number}"


def generate_ksef_qr(ksef_number: str, xml_filename: str) -> Path:
    stem = Path(xml_filename).stem
    qr_path = config.FINAL_PDF_DIR / f"{stem}_KSeF_QR.png"

    payload = build_ksef_qr_payload(ksef_number)

    img = qrcode.make(payload)
    qr_path.parent.mkdir(parents=True, exist_ok=True)
    img.save(qr_path)

    return qr_path

===== FILE END: sender/qr_code.py =====


===== FILE START: sender/send_invoice.py =====

from pathlib import Path
import base64
import hashlib


def prepare_invoice_payload(xml_path: Path):
    xml_path = Path(xml_path)

    if not xml_path.exists():
        raise FileNotFoundError(xml_path)

    xml_bytes = xml_path.read_bytes()
    xml_text = xml_bytes.decode("utf-8")

    payload = {
        "filename": xml_path.name,
        "size_bytes": len(xml_bytes),
        "sha256": hashlib.sha256(xml_bytes).hexdigest(),
        "invoice_text": xml_text,
        "invoice_base64": base64.b64encode(xml_bytes).decode("ascii"),
    }

    return payload

===== FILE END: sender/send_invoice.py =====


===== FILE START: sender/send_manifest.py =====

import json

from pathlib import Path
from datetime import datetime, timezone

import hashlib

import config
from sender.final_pdf import create_final_pdf_with_ksef_number
from sender.email_package import create_email_package


MANIFEST_PATH = config.OUTBOUND_DIR / "send_manifest.json"


def utc_now():
    return datetime.now(timezone.utc).isoformat()


def load_manifest() -> dict:
    if not MANIFEST_PATH.exists():
        return {
            "created_at": utc_now(),
            "updated_at": utc_now(),
            "env": config.KSEF_ENV,
            "items": []
        }

    return json.loads(MANIFEST_PATH.read_text(encoding="utf-8"))


def save_manifest(manifest: dict) -> None:
    manifest["updated_at"] = utc_now()
    MANIFEST_PATH.parent.mkdir(parents=True, exist_ok=True)
    MANIFEST_PATH.write_text(
        json.dumps(manifest, ensure_ascii=False, indent=2),
        encoding="utf-8"
    )

def mark_invoice_accepted(
    filename: str,
    ksef_number: str,
    invoice_reference: str,
    session_reference: str,
    response: dict,
):

    manifest = load_manifest()

    accepted_at = utc_now()
    xml_hash = calculate_xml_hash(filename)

    for item in manifest["items"]:
        if item["filename"] == filename:

            item["status"] = "accepted"

            item["ksef_number"] = ksef_number
            item["accepted_at"] = accepted_at
            item["ksef_hash"] = xml_hash

            item["invoice_reference"] = invoice_reference
            item["session_reference"] = session_reference

            item["last_response"] = response
            item["updated_at"] = accepted_at

            item.setdefault("history", []).append({
                "at": accepted_at,
                "status": "accepted",
                "note": "Faktura przyjęta przez KSeF"
            })

            save_manifest(manifest)

            final_pdf = create_final_pdf_with_ksef_number(
                xml_filename=filename,
                ksef_number=ksef_number
            )

            print("Final PDF:", final_pdf)
            
            email_dir = create_email_package(
            filename=filename,
            ksef_number=ksef_number
            )

            print("Email package:", email_dir)

            return item

def calculate_xml_hash(filename: str) -> str:

    xml_path = config.SENT_DIR / filename

    data = xml_path.read_bytes()

    return hashlib.sha256(data).hexdigest()

def upsert_invoice(
    filename: str,
    status: str,
    xml_path: str | None = None,
    pdf_path: str | None = None,
    sha256: str | None = None,
    size_bytes: int | None = None,
    note: str | None = None,
    ksef_number: str | None = None,
    invoice_reference: str | None = None,
    session_reference: str | None = None,
    response: dict | None = None,
) -> dict:
    manifest = load_manifest()

    # Blokada duplikatu po nazwie pliku
    item = None

    for existing in manifest["items"]:
        if existing["filename"] == filename:
            if existing.get("status") == "accepted":
                raise RuntimeError(
                    f"Faktura {filename} ma już numer KSeF — ponowna wysyłka zablokowana."
                )

            item = existing
            break

    # Blokada duplikatu po SHA256, nawet jeśli plik ma inną nazwę
    if sha256 is not None:
        for existing in manifest["items"]:
            if (
                existing.get("sha256") == sha256
                and existing.get("status") == "accepted"
                and existing.get("filename") != filename
            ):
                raise RuntimeError(
                    f"Ten XML był już zaakceptowany w KSeF jako {existing.get('filename')} — "
                    "ponowna wysyłka zablokowana."
                )

    if item is None:
        item = {
            "filename": filename,
            "created_at": utc_now(),
            "history": []
        }
        manifest["items"].append(item)

    item["status"] = status
    item["updated_at"] = utc_now()

    if xml_path is not None:
        item["xml_path"] = xml_path

    if pdf_path is not None:
        item["pdf_path"] = pdf_path

    if sha256 is not None:
        item["sha256"] = sha256

    if size_bytes is not None:
        item["size_bytes"] = size_bytes

    if note is not None:
        item["note"] = note

    if ksef_number is not None:
        item["ksef_number"] = ksef_number

    if invoice_reference is not None:
        item["invoice_reference"] = invoice_reference

    if session_reference is not None:
        item["session_reference"] = session_reference

    if response is not None:
        item["last_response"] = response

    item.setdefault("history", []).append({
        "at": utc_now(),
        "status": status,
        "note": note
    })

    save_manifest(manifest)
    return item

===== FILE END: sender/send_manifest.py =====


===== FILE START: sender/session_ledger.py =====

import json
from datetime import datetime, timezone

import config


LEDGER_PATH = config.OUTBOUND_DIR / "session_ledger.json"


def utc_now():
    return datetime.now(timezone.utc).isoformat()


def load_ledger() -> dict:
    if not LEDGER_PATH.exists():
        return {
            "created_at": utc_now(),
            "updated_at": utc_now(),
            "env": config.KSEF_ENV,
            "events": []
        }

    return json.loads(LEDGER_PATH.read_text(encoding="utf-8"))


def save_ledger(ledger: dict) -> None:
    ledger["updated_at"] = utc_now()
    LEDGER_PATH.parent.mkdir(parents=True, exist_ok=True)
    LEDGER_PATH.write_text(
        json.dumps(ledger, ensure_ascii=False, indent=2),
        encoding="utf-8"
    )


def log_event(
    event_type: str,
    status: str,
    filename: str | None = None,
    session_reference: str | None = None,
    invoice_reference: str | None = None,
    ksef_number: str | None = None,
    response: dict | None = None,
    error: str | None = None,
) -> dict:
    ledger = load_ledger()

    event = {
        "at": utc_now(),
        "env": config.KSEF_ENV,
        "event_type": event_type,
        "status": status,
        "filename": filename,
        "session_reference": session_reference,
        "invoice_reference": invoice_reference,
        "ksef_number": ksef_number,
        "response": response,
        "error": error,
    }

    ledger["events"].append(event)
    save_ledger(ledger)

    return event

===== FILE END: sender/session_ledger.py =====


===== FILE START: tools/extract_token_cert.py =====

import base64
import json
from pathlib import Path


INPUT = Path("public_keys.json")
OUTPUT = Path("keys/ksef_public_key.pem")


def wrap_pem(b64: str, title: str = "CERTIFICATE") -> str:
    lines = [b64[i:i + 64] for i in range(0, len(b64), 64)]
    return f"-----BEGIN {title}-----\n" + "\n".join(lines) + f"\n-----END {title}-----\n"


data = json.loads(INPUT.read_text(encoding="utf-8"))

selected = None

for item in data:
    usage = item.get("usage", [])
    if "KsefTokenEncryption" in usage:
        selected = item
        break

if not selected:
    raise RuntimeError("Nie znaleziono certyfikatu KsefTokenEncryption")

cert_b64 = selected["certificate"]

# sprawdzenie, czy Base64 jest poprawne
base64.b64decode(cert_b64)

OUTPUT.parent.mkdir(parents=True, exist_ok=True)
OUTPUT.write_text(wrap_pem(cert_b64), encoding="utf-8")

print("Zapisano:", OUTPUT)
print("validFrom:", selected.get("validFrom"))
print("validTo:", selected.get("validTo"))
print("usage:", selected.get("usage"))

===== FILE END: tools/extract_token_cert.py =====


===== FILE START: tools/inspect_keys.py =====

import json

with open("public_keys.json", "r", encoding="utf-8") as f:
    data = json.load(f)

print(type(data))

if isinstance(data, dict):
    for k, v in data.items():
        print("KEY:", k, "TYPE:", type(v))

print(json.dumps(data, ensure_ascii=False, indent=2)[:5000])

===== FILE END: tools/inspect_keys.py =====


===== FILE START: utils.py =====

import json
from pathlib import Path
from datetime import datetime, timezone

def save_json(path: Path, data: dict):
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(
        json.dumps(data, ensure_ascii=False, indent=2),
        encoding="utf-8"
    )

def utc_now():
    return datetime.now(timezone.utc).isoformat()

===== FILE END: utils.py =====


===== FILE START: validator/business_rules.py =====

import xml.etree.ElementTree as ET
from pathlib import Path
import config


def check_business_rules(xml_path: Path):

    result = {
        "file": str(xml_path),
        "ok": True,
        "errors": [],
        "warnings": []
    }

    try:
        tree = ET.parse(xml_path)
        root = tree.getroot()

    except Exception as e:
        result["ok"] = False
        result["errors"].append(f"Błąd parsowania XML: {e}")
        return result

    nip_sprzedawcy = None

    for el in root.iter():
        tag = el.tag.lower()

        if "nipsprzedawcy" in tag:
            nip_sprzedawcy = el.text

    if not nip_sprzedawcy:
        result["warnings"].append("Brak NIP sprzedawcy w XML")

    else:
        if nip_sprzedawcy != config.NIP:
            result["errors"].append(
                f"NIP sprzedawcy ({nip_sprzedawcy}) ≠ konfiguracja ({config.NIP})"
            )

    numer = None

    for el in root.iter():
        if "numerfaktury" in el.tag.lower():
            numer = el.text

    if not numer:
        result["warnings"].append("Brak numeru faktury")

    return result

===== FILE END: validator/business_rules.py =====


===== FILE START: validator/validate_fa.py =====

from pathlib import Path
import xml.etree.ElementTree as ET


def validate_xml_basic(xml_path: Path) -> dict:
    xml_path = Path(xml_path)

    result = {
        "file": str(xml_path),
        "ok": True,
        "errors": [],
        "warnings": [],
        "root_tag": None,
    }

    if not xml_path.exists():
        result["ok"] = False
        result["errors"].append("Plik XML nie istnieje.")
        return result

    try:
        tree = ET.parse(xml_path)
        root = tree.getroot()
        result["root_tag"] = root.tag

        if "Faktura" not in root.tag:
            result["warnings"].append(
                f"Nietypowy root tag XML: {root.tag}"
            )

    except ET.ParseError as e:
        result["ok"] = False
        result["errors"].append(f"Błąd parsowania XML: {e}")

    return result


def validate_pending_directory(pending_dir: Path) -> dict:
    pending_dir = Path(pending_dir)

    files = sorted(pending_dir.glob("*.xml"))
    results = []

    for xml_file in files:
        results.append(validate_xml_basic(xml_file))

    return {
        "pending_dir": str(pending_dir),
        "count": len(files),
        "valid_count": sum(1 for r in results if r["ok"]),
        "invalid_count": sum(1 for r in results if not r["ok"]),
        "results": results,
    }

===== FILE END: validator/validate_fa.py =====
